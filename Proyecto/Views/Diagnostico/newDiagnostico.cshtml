
﻿@model WebApplication1.Models.TblDiagnostico

@{
    ViewBag.Title = "newDiagnostico";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm("agregarDiagnostico", "Diagnostico", FormMethod.Post))
{
<div class="mb-3 mt-3">

    @Html.LabelFor(m => m.Titulo)
    @Html.TextBoxFor(m => m.Titulo, new { @class = "form-control" })
</div>

<div class="mb-3 mt-3">
    @Html.LabelFor(m => m.Descripcion)
    @Html.TextBoxFor(m => m.Descripcion, new { @class = "form-control" })
</div>

<button type="submit" class="btn btn-primary">Agregar Diagnostico</button>

}