@using WebApplication1.Models
@model List<RolesViewModel>
    @{
    ViewBag.Title = "Roles";
    }

    <h2>@ViewBag.Title</h2>
    <hr />


    @if (Model != null)
{
    <div class="container mt-3">
        <h2>Usuarios</h2>
        @Html.ActionLink("Nuevo", "newClinic", "Clinica", new { @class = "btn btn-primary" })
        <table class="table table-dark table-hover">
            <thead>
                <tr>
                    <th>Rol</th>
                    <th>Gestion</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                <tr>
                    <td>@item.ROL</td>
                    <td>
                        <input onclick="document.location.href='@Url.Content("~/Clinica/deleteClinic/" + item.id_rol)'" type="button" value="Eliminar" class="btn btn-danger" />
                        <input onclick="document.location.href='@Url.Content("~/Clinica/modifyClinic/" + item.id_rol)'" type="button" value="Editar" class="btn btn-warning" />
                    </td>
                </tr>
                }
            </tbody>
        </table>
    </div>
}
else
{
    <div class="container mt-3">
        <h2>No hay usuarios</h2>
    </div>
}
    <script>
    function EliminarUsuario(id) {
        if (confirm("Esta Seguro de Eliminar A Esta Clinica?????")) {
            url = "@Url.Content("~/Clinica/deleteClinic/")" + "/" + id;
            $.get(url).done((data)=>{
                if (data == 1) {
                    document.location.href = "@Url.Content("~/Clinica/Index")"
                } else {
                    alert("Ocurrio un Error :c");
                }
            });
        }
    }
    </script>
