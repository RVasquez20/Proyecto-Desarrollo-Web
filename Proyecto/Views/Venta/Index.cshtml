@using WebApplication1.Models
@model List<TblVenta>
@{
    ViewBag.Title = "Ventas";
}

<h2>@ViewBag.Title</h2>
<hr />


@if (Model != null)
{
    <div class="container mt-3">
        <h2>Ventas</h2>
        @Html.ActionLink("Nuevo", "newVenta", "Venta", new { @class = "btn btn-primary" })
        <table class="table table-dark table-hover">
            <thead>
                <tr>
                    <th>Serie</th>
                    <th>Numero</th>
                    <th>Fecha</th>
                    <th>Gestion</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Serie</td>
                        <td>@item.Numero</td>
                        <td>@item.Fecha</td>
                        <td>
                            <input onclick="document.location.href='@Url.Content("~/Venta/modifyVenta/" + item.IdVentas)'" type="button" value="Editar" class="btn btn-warning" />
                            <input onclick="document.location.href='@Url.Content("~/Venta/deleteVenta/" + item.IdVentas)'" type="button" value="Eliminar" class="btn btn-danger" />
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
else
{
    <div class="container mt-3">
        <h2>No hay Diagnosticos</h2>
    </div>
}
<script>
    function EliminarUsuario(id) {
        if (confirm("Esta Seguro de Eliminar A Esta Habitacion?????")) {
            url = "@Url.Content("~/Venta/deleteVenta/")" + "/" + id;
            $.get(url).done((data)=>{
                if (data == 1) {
                    document.location.href = "@Url.Content("~/Venta/Index")"
                } else {
                    alert("Ocurrio un Error :c");
                }
            });
        }
    }
</script>